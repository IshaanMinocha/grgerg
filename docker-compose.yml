services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    restart: always
  
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
